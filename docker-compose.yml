version: "3.9"
services:
  dtserver:
    build: .
    ports:
      - 3001:3001
    depends_on:
      - mysqldb

  mysqldb:
    image: mysql
    environment:
      - MYSQL_DATABASE=digitaltwins
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
      - MYSQL_ROOT_PASSWORD=password
    ports: 
      - 3306:3306
